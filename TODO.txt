
2 PARTS ->
    opengl renderer 
    partical simulator w/ spring mass system 

-> can kind of make them together 

-> the point of compatibility is the vertex list of the opengl renderer 
-> or the shader ? 

-> if we do it as a shader (its just a cloth sim after all, we dont need partical function by itself)
    then we dont need an explecit partical class, maby only a struct to pass to shader? idk
-> save the shader for a unity project?? (more applicibal)

data pipeline: 

origonal mesh ->

main loop ->

-> we have to write a partical simulator for the partical masses in spring mass system
vertex shader ->
    we have each vert data in vertex shader and can transform it 
    the verts posistion is saved with gl_Position built in var 

    do we need to pass the vert data back to the main program ? 
    -> only if we want to be able to bake the sim animation 
    -> to do this we dont need to preview the simulation 
    -> so slower computation is acceptiable, alowing to pass back data to the cpu 

    cloth sim functions have to be in the shader? 
    -> yes i think 
    -> set up (spring weights vertex weighs ect.) can be done outside of the shader then passed as uniform 
    -> saving gsgl variables inbetween loops?
    -> we have meny vertices to calculate so doing them in gsgl would be better preformace as it can do them in parrallel 
     -> this would mean that each vert couldnot be effected by any transformations of verts around it at time T
     -> would need some way to get info from time T-1 ie save variable in gsgl? 
        -> prob have to pass them back as uniform (can you do this?) and save in main program
        -> vars would be in matrix for each vert 

    -> full cloth sim function will be split between main program and vertex shader 

        -> anything that has to be saved in between loop (ie spring length) needs to be done in main function
         -> unless can figure out a way to pass back with buffers 

    -> use Transform Feedback to get previus vertex data or get it back in the main program 
    -> or shader storage buffer object?

    -> for colision we will need to be able to get the vertex of another object outside of the shader
    -> therfor at least some calulation need to be done outside the shader 
        -> could parrallel gpu program these calc to 
            -> COMPUTE SHADERS ? i dont think this would be easier then vertex shaders
        -> maby just start with single thread cpu first 

    -> start with cpu implimentation (could multi thread it) then use OPEN-CL for gpu implimentation
